# Prior Authorization Intelligence System — Phase 6
# Streamlit App Requirements
# Last updated: 2026-06-01

# Core app
streamlit>=1.32.0

# Data
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (must match training environment for pickle compatibility)
scikit-learn>=1.3.0

# Visualization (used in notebook visuals; optional for app)
matplotlib>=3.7.0
seaborn>=0.12.0

# SHAP (used in Phase 5 notebooks; optional for app)
shap>=0.44.0

# Utilities
python-dateutil>=2.8.0

# ── Installation ──────────────────────────────────────────────────────────
# pip install -r requirements.txt
#
# ── Run app ───────────────────────────────────────────────────────────────
# cd "Prior Authorization Intelligence System"
# streamlit run app/streamlit_app.py
#
# ── Notes ─────────────────────────────────────────────────────────────────
# Models are pre-trained pickle files in app/models/.
# Do NOT retrain models inside the app.
# Python >= 3.10 recommended for sklearn compatibility.
