# Prior Authorization Intelligence System — Phase 6
# Streamlit App Requirements
# Python 3.11 (see runtime.txt in repo root)
# Pinned for pickle compatibility with pre-trained models (scikit-learn 1.5.2 / numpy 1.x)
# DO NOT upgrade scikit-learn or numpy without retraining models from train_models.py.

# Core app
streamlit==1.35.0

# Data — numpy pinned to 1.x; numpy 2.x breaks sklearn 1.5.x pickle deserialization
pandas==2.2.2
numpy==1.26.4

# Machine learning — must match training environment exactly for pickle compatibility
# Models were trained and pickled with scikit-learn 1.5.2 (Python 3.11)
scikit-learn==1.5.2

# Visualization (used in app model performance section and Phase 5 notebooks)
matplotlib==3.8.4
seaborn==0.13.2

# SHAP — pinned to 0.45.x; shap>=0.46 requires numpy>=2.0 which conflicts above
# Note: shap is used in training notebooks only, not in the live app
shap==0.45.1

# Utilities
python-dateutil==2.9.0

# ── Local installation ──────────────────────────────────────────────────────
# pip install -r requirements.txt
#
# ── Run app ─────────────────────────────────────────────────────────────────
# cd "Prior Authorization Intelligence System"
# streamlit run app/streamlit_app.py
#
# ── Notes ───────────────────────────────────────────────────────────────────
# Models are pre-trained pickle files in app/models/.
# Do NOT retrain models inside the app.
# Python 3.11 required — scikit-learn 1.5.2 pickle files are Python 3.11 compatible.
