# Prior Authorization Intelligence System — App Requirements
# Runtime packages only. shap and seaborn are NOT imported by the live app.
# scikit-learn pinned to 1.5.2 — must match training environment exactly.

streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.4,<2.0
scikit-learn==1.5.2
matplotlib>=3.8.0
Pillow>=10.4.0
python-dateutil>=2.9.0
